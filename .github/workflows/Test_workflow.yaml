name: HelloWorld123

on: 
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-lone script
        run: echo "Hello Github Actions!"
  second-job:
    runs-on: windows-latest
    steps:
      - run: echo "this job is running on ${{runner.os}}"
      
